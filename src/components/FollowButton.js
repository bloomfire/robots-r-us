import React from 'react';
import Button from 'react-bootstrap/Button';

function FollowButton() {
	return (
		<div>
			<Button variant="primary" value="follow">
				Follow
			</Button>
		</div>
	);
}

export default FollowButton;
